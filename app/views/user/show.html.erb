<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>SeatYourself</title>
  </head>
  <body>
    <h1><%= Welcome @user.name %>!</h1>
    <p><%= link_to "Home", restaraunts_path %></p>
    <p><%= link_to "Edit", "/users/#{user.id}/edit" %></p>
    <p><%= link_to "Delete" @user, method: :delete, data: {confirm: "Are you sure you want to delete this product?"} %></p>

    <h3>Reviews</h3>

<p>Reviews for <%= @restaurant.name %></p>

<% @product.reviews.each do |review| %>
<p>
<%= review.comment %>
<br>
 <% if review.user != nil %>
  <em> by <%= review.user.email %></em>
   Added on: <%= review.created_at %>
  <% end %>
</p>
<% end %>

<% if current_user %>
  </body>
</html>
