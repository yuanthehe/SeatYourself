<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>SeatYourself</title>
  </head>
  <body>
    <h1>Welcome <%= @user.name %>!</h1>
    <p><%= link_to "Home", restaurants_path %></p>
    <p><%= link_to "Update Settings", "/users/#{@user.id}/edit" %></p>

    <h3><%= @user.name %>'s Reviews</h3>

    <p>Reviews for <%= @user.name %></p>
       <% @user.reviews.each do |review| %>
    <p>
       <%= review.content %>
    <br>
        <% if review.user != nil %>
          <em> by <%= review.user.email %></em>
          Added on: <%= review.created_at %>
        <% end %>
    </p>
      <% end %>

          <% if current_user %>
          <% end %>

  </body>
</html>
